  
- 项目名称：Maxellabs 3D Engine
- 描述：Maxellabs 3D Engine 是一个基于 WebGL 的 3D 引擎，支持多平台，多设备，多浏览器。
- 项目结构：
- 1. packages/engine：3D 引擎的核心代码
    - 1.1 packages/engine/src/engine：3D 引擎的入口文件
- 2. packages/rhi：渲染接口，支持多种渲染 API，如 WebGL，WebGL2，OpenGL，DirectX 等
    - 2.1 packages/rhi/src/webgl：WebGL 渲染接口
    - 2.2 packages/rhi/src/webgl2：WebGL2 渲染接口
    - 2.3 packages/rhi/src/webgpu：WebGPU 渲染接口
- 3. packages/core：核心库，提供 3D 引擎的基础功能，如场景管理，物体管理，光源管理，材质管理，动画管理等
    - 3.1 packages/core/src/scene：场景管理
    - 3.2 packages/core/src/object：物体管理
    - 3.3 packages/core/src/light：光源管理
    - 3.4 packages/core/src/material：材质管理
    - 3.5 packages/core/src/animation：动画管理
    - 3.6 packages/core/src/audio：音频管理
    - 3.7 packages/core/src/ui：UI 管理
    - 3.8 packages/core/src/input：输入管理
    - 3.9 packages/core/src/physics：物理管理
- 4. packages/math：数学库，提供 3D 引擎的数学运算功能
- 5. packages/physics：物理库，提供 3D 引擎的物理运算功能
- 6. packages/animation：动画库，提供 3D 引擎的动画管理功能
- 7. packages/audio：音频库，提供 3D 引擎的音频管理功能
- 8. packages/ui：UI 库，提供 3D 引擎的 UI 管理功能
- 使用 ESLint 进行代码风格检查，遵循 Airbnb 规范
- 变量和函数名使用 camelCase 命名法
- 类名使用 PascalCase 命名法
- 文件名使用 camelCase 命名法
- 私有变量不使用 _ 开头
- 减少使用 getter 和 setter 方法
- 状态管理：Redux Toolkit
- UI 组件库：Ant Design
- 样式方案：Styled Components
- 使用 Jest 和 React Testing Library 编写单元测试和集成测试
- 测试覆盖率达到 95% 以上
- 使用 try...catch 语句捕获和处理异常
- 对异步操作使用 Promise 或 async/await
- 使用 JSDoc 编写函数注释
- 每个组件必须包含 README.md 文件，说明组件的用途和 API

1. 私有变量不使用下划线()前缀
2. 使用getXXX()方法替代getter
3. 移除所有包含下划线()的变量名
4. 使用setXXX()方法替代setter
