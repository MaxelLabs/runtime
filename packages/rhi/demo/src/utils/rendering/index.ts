/**
 * rendering/index.ts
 * 渲染工具模块导出
 */

// 类型导出
export type { RenderTargetOptions, RenderTargetResources, RHIRenderPassDescriptor } from './types';

// RenderTarget 导出
export { RenderTarget } from './RenderTarget';
